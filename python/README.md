# lolcomp/Python: LOLCODE -> Python
The goal here is to go from LOLCODE to a Pythonic AST (`ast`), to compilation and execution of Python code. 

Do we want a step going through transpilation? For fun and grins?

